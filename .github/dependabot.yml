# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2

# 注册表配置
registries:
  # GitHub Packages 用于 @napgram/* scope 的内部包
  github-packages:
    type: npm-registry
    url: https://npm.pkg.github.com
    token: ${{secrets.NPM_TOKEN}}

updates:
  # workspace 根目录 (pnpm lockfile) - @napgram/* 走 GitHub Packages
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 10
    target-branch: "dev"
    registries:
      - github-packages
    
    # 显式指定 Commit 风格
    commit-message:
      prefix: "chore(deps)"
      prefix-development: "chore(deps-dev)"
    
    # 依赖分组
    groups:
      production-deps:
        patterns:
          - "@fastify/*"
          - "@mtcute/*"
          - "fastify"
        update-types:
          - "minor"
          - "patch"
      
      dev-deps:
        patterns:
          - "@types/*"
          - "typescript"
          - "vitest"
          - "eslint"
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"
    


  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 10
    target-branch: "dev"
    commit-message:
      prefix: "chore(deps)"
    groups:
      all-actions:
        patterns:
          - "*"
