{
  "name": "@naplink/napgram-gateway-client",
  "version": "0.0.1",
  "description": "NapGram Gateway Node.js WebSocket client.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NapLink/NapGram.git",
    "directory": "packages/gateway-client"
  },
  "keywords": ["napgram", "gateway", "client", "websocket", "sdk"],
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test test/*.test.mjs",
    "prepack": "pnpm build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@naplink/napgram-gateway-protocol": "^0.0.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
