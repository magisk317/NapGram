{
  "name": "@naplink/napgram-gateway-protocol",
  "version": "0.0.1",
  "description": "NapGram Gateway protocol types (frames/events/actions).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NapLink/NapGram.git",
    "directory": "packages/gateway-protocol"
  },
  "keywords": ["napgram", "gateway", "protocol", "websocket", "sdk"],
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "prepack": "pnpm build"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
