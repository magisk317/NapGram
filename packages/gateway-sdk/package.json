{
  "name": "@naplink/napgram-gateway-sdk",
  "version": "0.0.1",
  "description": "NapGram Gateway SDK (client + protocol).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NapLink/NapGram.git",
    "directory": "packages/gateway-sdk"
  },
  "keywords": ["napgram", "gateway", "sdk", "websocket"],
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "prepack": "pnpm build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@naplink/napgram-gateway-client": "^0.0.1",
    "@naplink/napgram-gateway-protocol": "^0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
